#include <iostream>
//#include <allegro.h> w przysz³oœci jak siê dowiem jak dodac biblioteke do visuala dodam dzwiêk do swojego programu 
//w czasie serii inny dzwiek,w menu inny,w czasie przerw inny
#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <time.h>
#include <windows.h>
using namespace std;


void czekaj(int milisekund) 
{ 
clock_t start = clock();
while(clock() < start+milisekund);
}

//void ustawienia(void) 
// jak na mechatro naucz¹ nas operacji na plikach to zrobie mo¿liwoœæ ustawiania
//czasu , iloœci serii ,czasu trwania itp

void przerwa (int czas,int nr,int serie)//czas podawany w sekundach,numer serii,iloœæ serii
{
	cout <<"\n\n Czas na przerwê :)\n";
	for (int i=1;i<czas;i++)
	{
		_sleep(1000);
		cout<<czas-i<<" sek. do konca przerwy i jedziemy z "<<nr+1<<" seria.Jeszcze tylko "<<serie-nr<<" serii"<<endl;
		if(i>czas-7)
		{
			Beep(8000-100*i,100);//czêstotliwoœæ,czas
		}
	}
	cout <<"\nKoniec przerwy.Jedziemy dalej\n";
}

void seria(int pow,int dlu,int nr,int serie)//ilosc powtorzen,czas trwania powtorzenia,numer serii,iloœæ serii
{
	for (int i=1;i<pow+1;i++)
	{
		cout <<"\nseria nr "<<nr<<"/"<<serie<< " powtórzenie nr "<<i<<"/"<<pow<<
			" #### pozosta³o "<<serie-nr<<" ser. i "<<pow-i+1<<" powt. do koñca serii";
					for (int j=0;j<serie+1;j++)//odliczanie sekund do koñca powtórzenia
					{
					_sleep(1000);
						cout <<"\n"<<dlu-j;
					}
					_sleep(1000);
	}
}

int main(void)
{
	setlocale(LC_ALL, "");
	//poni¿sza linijka wymaga biblioteki allegro,narazie nie umiem jej dodaæ do visuala
	// SAMPLE * dzwiek = load_sample( "dzwiek.wav" );
	float serie = 4;//iloœæ serii
	int pauza=60;//czas trwania przerwy w sekundach
	float powt=15;//ilosc powtorzen
	int tpowt=5;//czas trwania powtórzenia
	int z=3;
	int *tablicaserii = (int*) malloc (sizeof (int) * serie);
	while (z==3)
	{
				cout<< "przysiady czy stanie na palcach? \n"<<
					"a-przysiady \n"<<
					"s-stanie na palcach"<<
					"\nq-qniec "<<
					"\nCzarek mówi 4 serie po 15 powtórzeñ przez 5 sekund i potem minuta przerwy\n "
					<< "a siady 4 serie po 15 powtórzeñ i minuta przerwy\n";

				char wybor = getchar ();
					switch (wybor)
					{
					case 's'://stanie na palcach
						for (int i = 1;i< serie+1;i++)
						{
							cout<<"\nzaczynamy serie nr "<<i<<"########################################"<<endl;
						seria(powt,tpowt,i,serie);//ilosc powtorzen,czas trwania powtorzenia,numer serii,iloœæ serii
						przerwa (pauza,i,serie);//czas podawany w sekundach,numer serii,iloœæ serii;
						}
						break;
					case 'a'://przysiady
						{
							puts("zrób pierwsz¹ seriê a nastêpnie wcisnij klawisz by zrobiæ przerwê");

								for(int i = 0;i<serie-1;i++)
								{
									int n;
									cin>>n;
									przerwa (pauza,i+1,serie);
									time_t start_1 = time (NULL); // czas pocz¹tkowy
									cout <<endl<< "zaczynamy serie nr "<<i+2<<".Po jej skoñczeniu wciœnij dowolny klawisz aby kontynuowaæ"<<endl;
									cin>>n;
									system("pause");
									time_t stop_1 = time (NULL); // czas koñcowy
									float tser=difftime (stop_1, start_1);//czas serii
									cout <<endl<< "zrobienie serii nr "<<i+2<<" zaje³o Ci "<<tser<<" sekund.jeden przysiad zajo³ Ci œrednio "
										<<tser/powt<<" sekund"<<endl;
									tablicaserii[i]=tser;
								}

								for(int i = 0;i<serie-1;i++)//wypisanie czasów poszczególnych serii
								{
									cout <<endl<< "zrobienie "<<i+1<<" serii zaje³o Ci "<<tablicaserii[i]<<
										" sekund, srednio "<<tablicaserii[i]/powt<<" sekund na przysiad"<<endl;

								}break;

						}
					case 'q':
						{
							z=0;
							break;
						}

					default:
						{
						puts("wcisnij inny klawisz");
						}

						
					}
					break;
		}
system ("pause");
}